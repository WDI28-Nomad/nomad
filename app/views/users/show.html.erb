<!-- scripts needed for google maps -->
<script src="//maps.google.com/maps/api/js?v=3.23&key=AIzaSyATl1C376VZ4AGorIBLCyTTdc-19GynoJw&libraries=geometry"></script>
<script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
<script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script>
<!-- scripts needed for google maps -->

<div class="col-sm-3">
  <%= render 'sidebar' %>
</div>

<div class="col-sm-9" style="margin-left: -10px">
  <h1 class="page-title"><%= link_to "Plan A New Trip!", new_user_trip_path(@user, @trip), class: "btn btn-lg btn-success" %></h1>

  <!-- google map -->
  <div id="map" style='width: 100%; height: 550px;'></div>
  <!-- google maps javascript -->
  <script type='text/javascript'>
  $(document).on("ready page:load", function(){
    var triphash = <%=raw @hash.to_json %>;
    console.log(triphash);
      handler = Gmaps.build('Google');
      handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
        markers = handler.addMarkers(<%=raw @hash.to_json %>);
        handler.bounds.extendWith(markers);
        handler.getMap().setZoom(2);
      });
  });
  </script>


<br>

</div>
